# Suppress npm warnings about deprecated flags
# Railway uses --production which is deprecated, this silences the warning
loglevel=error

# Skip platform checks — lockfile generated on macOS arm64,
# Railway runs linux x64. esbuild optional binaries differ per platform.
force=true
